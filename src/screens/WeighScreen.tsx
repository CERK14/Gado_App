import { PlaceholderScreen } from './PlaceholderScreen';

export function WeighScreen() {
  return (
    <PlaceholderScreen
      emoji="⚖️"
      titulo="Pesar"
      descricao="Fluxo de pesagem em 3 passos (Etapa 6)."
    />
  );
}
